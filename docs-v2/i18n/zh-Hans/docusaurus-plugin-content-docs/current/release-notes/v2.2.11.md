---
sidebar_position: 5
---

# v2.2.11

发布日期：2026-02-07

## 重点

- 修复（Gemini）：在 `responseSchema` 场景下去除非 string 的 `enum`/`const` 约束（Gemini 会拒绝 boolean enum）。
- 体验：改进 `core` 命令对 `--provider/--model` 的参数处理，降低 `npx` 参数位置导致的错觉。
- Provider：对 Moonshot/Kimi 的模型约束做必要的运行时门禁（例如部分模型对 temperature 有强约束）。

