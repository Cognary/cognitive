---
sidebar_position: 3
---

# v2.2.9

发布日期：2026-02-07

## 重点

- 修复：`cog core run` 失败时会输出规范的 v2.2 error envelope（不再是 `Error: undefined`）。
- 打包：增加稳定的 `bin.js` 入口，发布/安装不依赖预先存在的 `dist/`。
- Core：stdin-core 占位符与运行时替换对齐，缺失字段按空处理（`query`/`code`）。

