---
sidebar_position: 1
---

# v2.2.13

- Fix: `--profile core` / `validate=off` no longer converts successful `ok: true` envelopes into `ok: false` error envelopes when `repair=on`.
- Docs: bump pinned `npx cogn@X.Y.Z` examples to `2.2.13`.

