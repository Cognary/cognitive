# Registry Fixture Module

This directory exists for conformance vectors only.

It simulates an installed module directory that includes `provenance.json`,
so the `--profile certified` policy gates can be exercised in offline tests.

